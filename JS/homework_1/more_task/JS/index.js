'use strict'

let sharm = 15;
let hurgada = 25;
let taba = 6;
let res = prompt('Ведите количество людей в вашей группе');
if (res = Number.isInteger(res) && res > 0) {
    if (res<=taba) {
        alert('')
    }