async - применяется тот скрипт который быстрее загрузился. 
останавливает рисовку. начинает применятся сразу после загрузки html. Применяется редко.


defer - загружает скрипты парал. с построением html. загружает все в порядке очередности.
 начинает подключать скрипты только после построения html.


-----------

var
let------------- переменные. "давай сделаем переменную".
const
'use strict'---- всегда пишу в JS. переход на JS6.

-----------

Типы данных в JS
в ковычках пишется слова без ковычек число. ковычки это строка.|
let bool = true (false); - переменная.                         |
let n = null; - пустая ячейка но она есть                      |   ------->простые типы данных                 
let a = undefined; - не знаешь информации которую надо вложить |

let obj = {                                                        |
name: ...,                                                         |
curency: ..., - объект. для описания. ключ: значение,              |-----------> сложные типы данных
}                                                                  |
                                                                   |
let b = Symbol ('a'); - используется как уникальній индентификатор.|

--------

методы просмотра результата кода
alert- вывести инфу на экраню чтоб показать инфу пользователю.
console.log (word)- вывести инфу в def tools для пазработчика.
typeof- проверка типа данных console.log(typeof w) (null не проверяет)

-----

методы получения инфы от пользователя

prompt- появляется окошко как input      let answer = prompt ('Enter your name') всегда показывается строкой
confirm- вопрос с ответом "да" или "нет"  let answer2 = confirm ('Are you happy?')

------

var x = 5;|
х = 20;   |  ----> перезаписать данные

------

JavaScript -однопоточный асинхронный язык программирования

-----

% остаток от деления

------
 
a= 2.55
b= 2.1
let c = (a * b).toFixed(3)  округление до 3 знака после запятой

------

способы превращения строки в число
1. перед строкой поставить "+"
с=+ (a+b)
console.log (+c)

2. console.log(Number*превратить в число* (c*что превратить*));

-----

увиличить переменную на 1
a++
если на 2
a+= 2

-------

вспомогательные сайты

www.medium.com-новости 
mdn - документация по JS
stackoverflow
w3school
devdocs.io
learn javascript
-------

способ выделения числа из строки

let font = '2rem'
console.log (parseInt(font)); только целые числа|
                                                |-----> работает только если строка начинается с числа
console.log (parseFloat(font)); и те и те числа.|

-------

isNan - проверить значение число это или нет

console.log(isNan(5)); (звучит как (это число? (5)))

-------

\ - ставим перед спецсимволом чтоб просто его напечатать а не как служебный символ.
способ называется экранирование

-------

.trim() убрать лишние пробелы в начале и конце строки

-------
