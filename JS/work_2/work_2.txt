let str = 'Hello world';|
console.log(str.length);|---------> длина строки

сonsole.log(str.toLowerCase()); -------> нижний регистр
сonsole.log(str.toUpperCase());  -------> верхний регистр
console.log(str.includes('world'); ------> поиск какого нибудь слова в строчек
cobsole.log(str[6]); ----> показать символ по 6-й по счету
console.log(str.indexof('l'); ------> какая по счету буква l. показывает первывй символ который нашел
console.log(str.split('если ставим пробел разибвает на слова');----->разбить строку на слова

let arr = str.split(' ');
console.log(arr)

let strNew = arr.join('пишем через то что хочем соеденить ');---->слепить слова обратно в строку

let str = 'ssqsjfkq qkwfjqwfq lqqwoqw';
console.log(str.slice(0,3)); ------------->от какого по какой символ обрезать.последний не включается.
console.log(str.substr(0,3)); --------> тоже обрезка только говорим с какого символа начинаем и сколько по счету символов обрезать
console.log(str.substring(0,3)); --------> аналогично с slice


Замена
сonsole.log(str.replace(название переменной (слово) которое изменить, 'на какое слово заменить');


Math
Для работы с математическими действиями
console.log(Math.floor(2.99));----> округление в меньшую
console.log(Math.ceil(2.000001));-----> округление в большую сторону
console.log(Math.round(2.49)); ----->округление по школьным правилам
console.log(Math.abs(10));-----> модуль (минусовіе превращает в плюсовіе числа)
console.log(Math.pow(число которое подносим в степен, в какую степень подносим));-------> степень
console.log(Math.sqrt(4));------>квадратный коренью если другой корень то через pow
console.log(Math.max(-50,-40));-----> показать большее из єтих двух чисел
console.log(Math.min(9/2,3/1));-----> показать меньшее из этих двух чисел
console.log(Math.random()*100);----> случайное число. изначально от 0 до 1. *100 дает от 1-100


сравнение пишется ===



логические операторы
&& (and) - если выполнятся и это и это и это условие
|| (or) - если выполнится или это или это или это условие

структуры контроля
циклы, функции, условия.
if, else

let time = prompt ('Enter time');
if (time <= 21) {
    alert ('Включи футбол 1');
} else {
    alert ('можешь идти спать')
}



Тернарный оператор

------

шаблонная строка пишеться чере ` а не '/
